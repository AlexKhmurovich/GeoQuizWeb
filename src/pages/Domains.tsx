import PlayView from "./PlayView";

export default function Domains() {
   return (
      <PlayView
         mode="Domains"
         questionString="Name the top level domain (TLD) of this country:"
      />
   );
}
